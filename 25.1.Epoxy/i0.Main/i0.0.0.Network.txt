A. Qemu/KVM

    Connection Details / Virtual Networks

    1. Management Network - NAT
       internet access
       10.0.0.0/24 DHCP enabled

    2. bridge
       no IPs, Neutron will provide

    Each VM should have 2 virtual NICs:

    1. eth0 Management Network - NAT
    2. eth1 bridge

    During startup, the read sequence will be:

    1. ifconfig.eth0 Management Network - NAT
    2. ifconfig.eth1 bridge   
